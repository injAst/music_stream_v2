name: music_stream_server
description: REST API for Pulse Music (PostgreSQL).
publish_to: "none"

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^2.14.1
  dotenv: ^4.2.0
  postgres: ^3.4.8
  shelf: ^1.4.2
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4
